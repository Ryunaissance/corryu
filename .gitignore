# 구 데이터 파일 (더 이상 사용 안 함)
*.pkl
*.pickle
correlation_matrix.csv
correlation_matrix_monthly.csv
data_processed/
data_scraped/
archive/

# raw/ parquet은 Git이 아니라 외부 스토리지(Supabase Storage)에 보관 (src/storage.py)
# git 히스토리 비대화 방지 — daily 파이프라인이 pull/push로 동기화
raw/*.parquet

# 개별 ETF JSON — Vercel 빌드 시 build_etf_pages.py가 etf_data.json으로부터 자동 생성
output/etf-data/

# 시스템
.DS_Store
__pycache__/
*.pyc
*.pyo
.vercel/
.claude/
node_modules/
.venv/
.env
.ipynb_checkpoints/
*.log
